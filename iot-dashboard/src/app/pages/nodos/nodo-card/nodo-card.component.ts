import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'ngx-nodo-card',
  // changeDetection: ChangeDetectionStrategy.OnPush,
  templateUrl: './nodo-card.component.html',
})

export class NodoCardComponent {
}
